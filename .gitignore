# Binaries
secrets-dispatcher
.build/
mock-secret-service
*.exe

# Frontend build artifacts
web/dist/
web/node_modules/

# Embedded frontend: internal/api/web/dist is COMMITTED (keeps plain
# `go install` fully functional, web UI included). Regenerate with
# `make embed-frontend`; CI fails if it drifts from web/ sources.

# Test artifacts
web/test-results/
